# Banco de dados (PostgreSQL na VPS)
DATABASE_URL=postgresql://usuario:senha@ip-da-vps:5432/finapp

# JWT
JWT_SECRET=troque_por_uma_chave_secreta_longa

# API
PORT=3000

# Mobile — URL da API (Railway em produção, localhost em dev)
API_URL=http://localhost:3000
